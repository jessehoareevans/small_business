<%= render "layouts/errors", :object => @review %>

 <%= form_for [@product, @review] do |f| %>
   <%= f.label :author %>
   <%= f.text_field :author %>
   <%= f.label :comment %>
   <%= f.text_field :comment %>
   <%= f.submit %>
 <% end %>
